# DLinear 多变量时序预测 — 调参实验报告

> 生成脚本: `scripts/DLinear_tuning/generate_report.py`  
> 数据来源: `results/results.csv` (共 96 次训练)  
> 最优组合: `results/best_per_task.csv`

---

## 1. 实验目的

在 DLinear 多变量 (`--features M`) 设置下, 通过网格搜索 `learning_rate` × `dropout` 两个超参数, 在 ETTh1 (enc_in=7)、Electricity (enc_in=321)、Weather (enc_in=21) 三个数据集上、预测长度 96 与 192 上, 找到每个 (数据集, 预测长度) 任务的最优参数组合, 并与 DLinear 论文的默认参数 (lr=0.0001, dropout=0.05) 做对比。
`enc_in` 设为该数据集原始通道数,模型采用 DLinear-S (共享 Linear),即所有变量共用同一套 `seq_len → pred_len` 线性映射。

---

## 2. 模型介绍

**DLinear** (AAAI 2023, Zeng et al., https://arxiv.org/pdf/2205.13504v2) 是一个**多变量预测**模型。它将输入序列用 `moving_avg` (kernel_size=25) 分解为**趋势项 (trend)** 和**季节项 (seasonal)**,然后对两部分分别用 Linear 层做 `seq_len → pred_len` 的线性映射,最后叠加输出。结构极简 (无 attention, 无 RNN, 也**无 Dropout**),但论文证明在多个基准上击败了 Transformer 系列。
本实验使用 **DLinear-S (Shared)** 变体 (论文默认),即所有变量共用同一套 Linear;另一种变体 DLinear-I (Individual) 为每个变量配独立 Linear,本次未做对比。

**公式:**  `X = X_trend + X_season`;  `Y = Linear_trend(X_trend) + Linear_season(X_season)`

**输入**: `seq_len=336`;  **预测**: `pred_len ∈ {96, 192}`;  **优化器**: Adam;  **损失**: MSE;  **early stopping patience = train_epochs = 10**;  **enc_in** 随数据集变化 (ETTh1=7 / Electricity=321 / Weather=21)。

---

## 3. 最优参数设置 (按 MSE 最小)

| 数据集 | pred_len | learning_rate | dropout | mse | mae | mape | msmape (%) |
|---|---|---|---|---|---|---|---|
| electricity | 96 | 0.001 | 0.0 | 0.14008 | 0.2374 | 2.1484 | 35.5443 |
| electricity | 192 | 0.001 | 0.0 | 0.15379 | 0.2505 | 2.3025 | 37.2584 |
| etth1 | 96 | 0.001 | 0.0 | 0.37657 | 0.3982 | 9.0010 | 57.2575 |
| etth1 | 192 | 0.005 | 0.0 | 0.40409 | 0.4125 | 8.6389 | 58.7932 |
| weather | 96 | 0.0005 | 0.0 | 0.17357 | 0.2329 | 10.7898 | 40.2199 |
| weather | 192 | 0.001 | 0.0 | 0.21599 | 0.2739 | 10.7833 | 46.8604 |

---

## 4. 与默认参数 (lr=0.0001, dropout=0.05) 基线对比

> 提升百分比 = (基线 - 最优) / 基线 × 100,负值表示**变差**。

| 数据集 | pred_len | 基线 MSE | 最优 MSE | MSE 改善 | 基线 MAE | 最优 MAE | MAE 改善 |
|---|---|---|---|---|---|---|---|
| electricity | 96 | 0.14698 | 0.14008 | +4.70% | 0.2479 | 0.2374 | +4.25% |
| electricity | 192 | 0.16090 | 0.15379 | +4.42% | 0.2611 | 0.2505 | +4.05% |
| etth1 | 96 | 0.39838 | 0.37657 | +5.47% | 0.4177 | 0.3982 | +4.67% |
| etth1 | 192 | 0.43158 | 0.40409 | +6.37% | 0.4379 | 0.4125 | +5.81% |
| weather | 96 | 0.17448 | 0.17357 | +0.52% | 0.2345 | 0.2329 | +0.68% |
| weather | 192 | 0.21769 | 0.21599 | +0.78% | 0.2777 | 0.2739 | +1.39% |

---

## 5. 实验结果分析

### 5.1 超参数敏感性 — learning_rate

固定 `dropout`,展示每个 (数据集, pred_len) 在 4 个学习率下的 MSE (同一 lr 的 4 个 dropout 值取均值,因为 dropout 对 DLinear 无效)。

| 数据集 | pred_len | lr=1e-4 | lr=5e-4 | lr=1e-3 | lr=5e-3 |
|---|---|---|---|---|---|
| electricity | 96 | 0.14698 | 0.14059 | 0.14008 | 0.14012 |
| electricity | 192 | 0.16090 | 0.15438 | 0.15379 | 0.15381 |
| etth1 | 96 | 0.39838 | 0.37826 | 0.37657 | 0.38414 |
| etth1 | 192 | 0.43158 | 0.42516 | 0.40632 | 0.40409 |
| weather | 96 | 0.17448 | 0.17357 | 0.17565 | 0.17461 |
| weather | 192 | 0.21769 | 0.22337 | 0.21599 | 0.21695 |

### 5.2 超参数敏感性 — dropout

**关键发现:** 全部 24 个 (数据集, pred_len, lr) 组里, 有 24 组 (100%) 在 4 个 dropout 值下得到**完全相同的 MSE**。原因:`models/DLinear.py` 中**没有 `nn.Dropout` 层**, `--dropout` 命令行参数对 DLinear 没有任何作用。

因此,本次 DLinear 实验真正起作用的可调超参数**只有 `learning_rate` 一个**。若改为 Transformer 系列模型 (Informer/Autoformer),dropout 才会真正影响性能。

### 5.3 三个数据集的预测难度

对比 ETTh1、Electricity、Weather 三个数据集在 M 模式下的最优 MSE 量级,评估数据特性:

| 数据集 | enc_in (原始通道数) | 采样间隔 | 数值范围 | 预测难度 |
|---|---|---|---|---|
| ETTh1 | 7 | 1 h | 油温,有明显日/周周期 | 中 |
| Electricity | 321 | 1 h | 用电量,高方差、长尾 | 高 |
| Weather | 21 | 10 min | 气象,短时噪声大 | 低-中 |

实际最优 MSE 也印证了这一点 (数值大小取决于数据本身,需结合量级判断):

| 数据集 | pl=96 最优 MSE | pl=192 最优 MSE |
|---|---|---|
| electricity | 0.14008 | 0.15379 |
| etth1 | 0.37657 | 0.40409 |
| weather | 0.17357 | 0.21599 |

---

## 6. 实验思考

### 6.1 多变量 (M) 预测的优劣 (本次实验采用)

- **本次实验使用 M 模式**:将每个数据集的所有通道 (enc_in 分别为 7/321/21) 全部作为输入,模型对所有通道一起预测。M 模式能利用通道间的相关性(例如不同客户的用电量互相影响,气象各要素耦合)。

- **M 模式的代价**:输入维度大,模型仍要保持轻量。S 模式 (单变量) 只用 OT 列自身历史,适合目标变量自身有显著自相关、对其他变量不敏感的场景。

- **DLinear-S 的特点**:所有通道共享同一套 Linear 权重,这意味着 DLinear-S 在 M 模式下参数量与 S 模式相同 (~130K),不会因为通道数变多而过拟合,也意味着它对通道间非线性关系建模能力有限。

- **M vs S 的选择**:DLinear 论文 Table 7/8 中 M 列与 S 列结果差异不大,说明 DLinear 在这两种模式下表现接近。若要充分利用通道间关系,可以换用 DLinear-I (每通道独立 Linear) 或 TSMixer/Transformer。

- **运营商网络场景**:流量/负载预测用多变量更合适 (基站之间存在空间相关性);单设备温度预测用单变量即可。

### 6.2 时序预测对运营商网络的作用

1. **流量预测**:对核心网流量、无线接入负载做短时预测,提前调度资源,避免拥塞。

2. **故障预警**:通过设备温度、CPU 利用率等指标预测异常,提前派单维护。

3. **节能优化**:基站 AAU/RRU 在低负载时进入休眠,需要精确预测负载曲线。

4. **容量规划**:长期预测用于基站选址、光纤扩容等。

5. **业务感知**:用户行为预测用于精准营销、智能客服。

### 6.3 MSMAPE 为何要修正 MAPE

- **MAPE** = `mean(|pred - true| / |true|)`,分母是 `|true|` 单独项。

- 当 `true → 0` 时,MAPE 分母接近 0,误差被无限放大,出现「幽灵大值」。对 `Electricity` 这种大量接近 0 的数据,MAPE 经常 > 1 (= > 100%)。

- **MSMAPE** = `mean(2 * |pred - true| / max(0.5+eps, |pred| + |true| + eps)) * 100`,做了两件事修正:

  1. 分母用 `|pred| + |true|` (sMAPE 风格) 而非 `|true|`,分子也乘 2,值域限制在近似 [0, 2] (即 [0, 200%])。

  2. 再用 `max(0.5+eps, ...)` 兜底,即使预测/真实都接近 0,分母也不会被 0 吞掉,误差被钳制在有限范围。

- 实验中也确实看到:weather/electricity 的 MAPE 常 > 1 (数倍),而 MSMAPE 始终 < 50% 更稳定。

